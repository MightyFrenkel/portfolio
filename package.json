{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --stream --parallel -r run dev",
    "build": "pnpm --stream -r run build",
    "start": "pnpm --filter @actuallyfrank/web start"
  }
}
